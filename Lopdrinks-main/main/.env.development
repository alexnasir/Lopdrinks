# Development environment — CRA proxy handles CORS.
# Leaving REACT_APP_API_URL empty means axios uses a relative base URL ("/"),
# so requests go to localhost:3000 and the proxy forwards them to the real API.
REACT_APP_API_URL=
